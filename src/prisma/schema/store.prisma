model Store {
  id        Int       @id @default(autoincrement())
  name      String    @unique
  email     String?
  address   String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

 @@map("store")
}